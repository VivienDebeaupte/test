{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                <div class="col float-start">
                    <a href="{{ path('cart_index') }}">Retour à ma commande</a>
                </div>
                <div class="col mx-auto" style="width: 30%;">
                    <h3 class="">Votre montant à payer est de {{ cost|number_format(2, ',', ' ') }} €</h3>
                </div>
            </div>
        </div>
    </div>
{% endblock %}